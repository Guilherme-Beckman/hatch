# Sprite generation environment variables
# Copy this file to .env and fill in the values you need.

# ── Hugging Face (optional) ───────────────────────────────────────────────────
# Required only when running:  npm run sprites:generate -- --provider huggingface
# Get a free token at:  https://huggingface.co/settings/tokens
HF_TOKEN=hf_...

# ── Pollinations.AI (optional) ────────────────────────────────────────────────
# The default provider works WITHOUT any token (anonymous, 15s between requests).
# If you sign up for a free account at https://auth.pollinations.ai you can
# get a publishable key that removes rate limits. Uncomment to use it:
# POLLINATIONS_TOKEN=pk_...
