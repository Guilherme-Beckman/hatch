# Sprite generation environment variables
# Copy this file to .env and fill in the values you need.

# ── Google Gemini (recommended) ───────────────────────────────────────────────
# Required for:  npm run sprites:generate -- --provider gemini
# Free: 500 images/day, no billing needed
# Get free key at:  https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...

# ── Hugging Face (alternative) ───────────────────────────────────────────────
# Required for:  npm run sprites:generate -- --provider huggingface
# Get a free token at:  https://huggingface.co/settings/tokens
HF_TOKEN=hf_...

# ── Pollinations.AI (default, no key needed) ──────────────────────────────────
# Works WITHOUT any token (15s between requests, ~10 min total for 36 sprites).
# Optional: sign up free at https://auth.pollinations.ai to remove rate limits.
# POLLINATIONS_TOKEN=pk_...
