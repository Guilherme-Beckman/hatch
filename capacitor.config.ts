import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.birdwatch.hatch',
  appName: 'hatch',
  webDir: 'dist/hatch/browser'
};

export default config;
